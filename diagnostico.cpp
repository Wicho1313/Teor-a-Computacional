#include <stdio.h>
#include <conio.h>
#include <stdlib.h>
void imprimir (char cadena[]);
int main()
{
	int n,i;      //declaramos n que sera el valor que vamos a ingresar para darle al vector
	char cadena[n];
	char arreglo[cadena];
	printf("Ingrese el tamanio del arreglo: ");
	scanf("%d",&n);
	for(i=0;i<=n;i++){
		fflush(stdin);
	printf("escriba la frase: ");
    gets(cadena[i].arreglo);	
	}
for(i=0;i<=n;i++){
printf(" %s *",cadena[i].arreglo);
}
	}


 
